# Ambiente de replicacao — Art. 3 (Ambiente & Sociedade)
# Python 3.11
#
# Confirme as versoes no ambiente que gerou os resultados finais:
#   pip freeze | grep -E "pandas|numpy|scipy|geopandas|shapely|pyproj|libpysal|esda|statsmodels|patsy|matplotlib|openpyxl"

# --- Nucleo ---
pandas==2.2.2
numpy==1.26.4

# CRITICO: statsmodels 0.14.2 importa scipy._lib._util._lazywhere, removido
# no scipy 1.14. Sem este pin, a importacao de statsmodels falha.
scipy==1.13.1

pyyaml==6.0.1

# --- Geoprocessamento ---
# A versao do pyproj determina a base PROJ usada nas transformacoes de CRS;
# divergencias alteram distancias e, portanto, os buffers e a matriz de pesos.
geopandas==1.0.1
shapely==2.0.5
pyproj==3.6.1
fiona==1.9.6
pyogrio==0.9.0

# --- Formato colunar (Parquet) ---
# A secao 5.1 grava previous_land_use_grid_year_long.parquet ao final; em uma
# segunda execucao a rota canonica le o Parquet antes do CSV. Sem pyarrow, a
# leitura falha.
pyarrow==16.1.0

# --- Analise espacial ---
libpysal==4.12.1
esda==2.6.0

# --- Modelagem ---
statsmodels==0.14.2
patsy==0.5.6

# --- Visualizacao e exportacao ---
matplotlib==3.9.0
openpyxl==3.1.5

# --- Notebooks ---
jupyter==1.0.0
nbstripout==0.7.1
